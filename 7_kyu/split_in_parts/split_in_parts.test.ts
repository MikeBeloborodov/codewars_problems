import { splitInParts } from "./split_in_parts";

describe("test cases", () => {
  it("should ", () =>
    expect(splitInParts("supercalifragilisticexpialidocious", 3)).toBe(
      "sup erc ali fra gil ist ice xpi ali doc iou s",
    ));
});
